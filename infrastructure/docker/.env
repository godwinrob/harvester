# WARNING: This file contains DEFAULT values for LOCAL DOCKER COMPOSE DEVELOPMENT ONLY
# These credentials are NOT secure and should NEVER be used in production
# For production deployments, use proper secrets management (Kubernetes Secrets, etc.)
# See .env.example for a template to copy

BUILD=develop
GOMAXPROX=2
BUILD_REF=0.0.3

# Database configuration - LOCAL DOCKER COMPOSE ONLY
# These match the postgres service defined in docker-compose.yml
HARVESTER_DB_HOST=postgres
HARVESTER_DB_USER=postgres
HARVESTER_DB_PASSWORD=postgres
HARVESTER_DB_NAME=postgres
HARVESTER_DB_DISABLE_TLS=true

# Database reset flag - drops all tables before migration
# Set to "true" for development to avoid migration checksum errors
# Set to "false" for production (NEVER reset production databases!)
HARVESTER_DB_RESET=true